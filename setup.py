from setuptools import setup


setup(
  name='Flask-Captain',
  description="Handle webhooks with Flask",
  version='0.0.1',
  author_name='Mark Steve Samson',
  author_email='hello@marksteve.com',
  py_modules=['flask_captain'],
  install_requires=[
    'Flask',
  ],
)
